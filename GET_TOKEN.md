# Renew LinkedIn Access Token

Token expires every **60 days**. To renew:

```bash
python get_token.py
```

This opens LinkedIn in your browser → authorize → copy the token → update `LINKEDIN_ACCESS_TOKEN` in GitHub Secrets.
